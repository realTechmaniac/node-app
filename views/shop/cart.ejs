<%- include('../includes/head.ejs') %>
<body>
    <%- include('../includes/navigation.ejs') %>
    <main>
        <% if(products.length > 0) { %>
            <ul>
                <% products.forEach(p => { %>
                    <l1><%= p.productData.title %> (<%= p.qty %>)</l1>
                <% }) %>
            </ul>
        <% } else { %>
            <h2>No Product Found</h2>
        <% } %>
    </main>

<%- include('../includes/end.ejs') %>